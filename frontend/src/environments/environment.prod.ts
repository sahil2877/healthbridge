// Production environment (used by `ng build --configuration production`).
// IMPORTANT: After deploying the backend on Render, replace the value below
// with your live backend URL, e.g. 'https://healthbridge-api.onrender.com'
// (NO trailing slash).
export const environment = {
  production: true,
  apiBase: 'https://YOUR-BACKEND.onrender.com',
};
